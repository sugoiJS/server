// import {ParametersValidatorUtil} from "./parameters-validator.util";
import { Policy} from "@sugoi/core";
// import {SugoiServerError} from "../exceptions/server.exception";
// import {EXCEPTIONS} from "../constants/exceptions.constant";

export class AuthorizationUtils {

    // @Policy()
    // static isAuthorized(args: {functionArgs: any[], policyMeta: [{ requiredRole: string | number }]}):Promise<boolean> {
        // const request = ParametersValidatorUtil.getRequestFromArgs(args.functionArgs);
        // if(!request){
        //     throw new SugoiServerError(EXCEPTIONS.REQUEST_ARGUMENT_NOT_FOUND.message,EXCEPTIONS.REQUEST_ARGUMENT_NOT_FOUND.code);
        // }
        // const inject(Symbol.for("AuthProvider"));
    // }

}